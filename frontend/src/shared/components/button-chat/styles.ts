import { SxProps } from "@mui/material";

export const buttonChat = {
  position: "fixed",
  width: "100%",
  left: -20,
  bottom: "50px",
  textAlign: "end",
  "& > :not(style)": { m: 1 }
} as SxProps;